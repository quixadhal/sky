// Autogenned file

inherit "/obj/food.c";

void setup(){

// #class
  add_adjective( "some" );
  set_amount_types( ([ "smidgen": ({ 1, "smigen" }),                          "lump": ({ 50,"lumps" })])  );
  add_adjective( ({ "smidgen", "smidgens", "lump", "lumps", "of" }) );
  set_pile_types( ({ 50, "small", 100, "medium", "large" })  );
  set_continuous( );
} /* setup() */
