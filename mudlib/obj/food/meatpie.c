// Autogenned file

inherit FOOD_OBJ;

void setup(){

  set_name("meatpie" );
  set_short("meat pie" );
  add_adjective(({ "meat", "tasty", "hot" }) );
  add_alias("pie" );
  set_main_plural("meat pies" );
  add_plural(({ "meatpies", "pies" }) );
  set_value(100 );
  set_weight(2 );
  set_long("This is one of the famous tasty Sheepridge meat "
      "pies. In fact, it's about the only thing Sheepridge is "    
      "famous for, or ever likely to be famous for.\n");
} /* setup() */
