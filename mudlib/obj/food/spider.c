// Autogenned file

inherit FOOD_OBJ;

void setup(){

  set_name("spider" );
  set_short("spider" );
  add_adjective(({ "fat", "hairy", "arachnid" }) );
  set_long("A fat, hairy, silk producing arachnid, which has mutated over "
   "years of exposure to the fumes from brewing into a truely awesome " 
   "monster.  Unfortunately this one has been slightly squished.\n" );
  add_alias(({"arachnid", "fat spider", "hairy spider", "fat and hairy spider"}) );
  add_plural(({"arachnids", "fat spiders", "hairy spiders",  "fat and hairy spiders"}) );
  set_weight(2 );
  set_main_plural("spiders" );
  set_medium_alias("Spider"  );
  set_weight_per_bite(2);
} /* setup() */
