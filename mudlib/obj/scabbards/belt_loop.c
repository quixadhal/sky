// Autogenned file

inherit SCABBARD_OBJ;

void setup(){

  set_name("loop" );
  add_adjective(({ "belt", "belt loop" }) );
  add_alias("belt loop" );
  set_short("belt loop" );
  set_long("This loop is made from the finest leather, designed to "
           "hold axes or hammers safely.\n" );
  set_weight(8 );
  set_value(800 );
  set_type("belt scabbard" );
  add_property("no recycling", 1 );
  setup_scabbard(10000 );
  set_damage_chance(15 );
  set_types(({ "axe","hammer" }));
} /* setup() */
