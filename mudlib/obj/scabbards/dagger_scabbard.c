// Autogenned file

inherit SCABBARD_OBJ;

void setup(){

  set_name("scabbard" );
  add_adjective("dagger" );
  set_short("dagger scabbard" );
  set_long("Similar in design to the scabbard made for longer bladed weapons."
           "  In fact you could say they were the same, except for the size.\n" );
  set_weight(3 );
  set_value(30 );
  set_type("small scabbard" );
  setup_scabbard(10000 );
  set_damage_chance(15 );
  set_types(({ "dagger", "knife" }));
} /* setup() */
