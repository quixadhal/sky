// Autogenned file

inherit CLOTHING_OBJ;

void setup(){

  set_name("bodice" );
  set_short("white cotton bodice" );
  add_adjective(({ "white", "cotton" }) );
  set_main_plural("white cotton bodices" );
  set_long("The plain white cotton bodice has a set of buttons running "
           "down the middle of the chest and ending at the waist.  The "
           "arms are puffed up at the top and are finished at the wrists "
           "with a simple plain white cuff.\n" );
  set_weight(8 );
  set_value(600 );
  set_type("shirt" );
  set_damage_chance(20 );
  setup_clothing(80);
} /* setup() */
