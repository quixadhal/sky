// Autogenned file

inherit CLOTHING_OBJ;

void setup(){

  set_name("hat" );
  set_short("brown felt hat" );
  add_adjective(({ "brown", "felt" }) );
  set_long("This is the sort of thing you'd wear on your "
     "head simply to because you might need to wear a hat.  "
     "It's cheap and not at all stylish and for some reason "
     "whenever someone wears one, they look a decade older.\n" );
  set_weight(4 );
  set_value(40 );
  set_type("hat" );
  setup_clothing(40 );
  set_damage_chance(20);
} /* setup() */
