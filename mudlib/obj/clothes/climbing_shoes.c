// Autogenned file

inherit CLOTHING_OBJ;

void setup(){

  set_name("shoes" );
  set_short("pair of climbing shoes" );
  add_adjective(({ "pair", "of", "climbing" }) );
  add_plural(({ "pairs of climbing shoes" }) );
  set_main_plural("pairs of climbing shoes" );
  set_long("These shoes are made from strong canvas and soft leather.  "
           "It's reasonable too choose them one size too small, in that case "         
           "you will have a perfect feeling for the structure of the rock "         
           "underneath your soles.\n" );
  set_material("leather" );
  set_weight(6 );
  set_value(1500 );
  set_type("boot" );
  setup_clothing(240 );
  set_damage_chance(15);
} /* setup() */
