// Autogenned file

inherit ARMOUR_OBJ;

void setup(){

  set_name("locket" );
  set_short("silver locket" );
  add_adjective("silver" );
  add_plural("jewellery" );
  add_alias( "jewellery" );
  set_main_plural("silver lockets" );
  set_long("This simple but elegant silver locket is engraved "
           "with a lovely flower motif, with the clasps of the "
           "fine silver chain on which it hangs formed to look "
           "like two tiny flowers.\n" );
  set_weight(2 );
  set_value(5000 );
  add_property("shop type", "jewellers" );
  set_type("necklace" );
  setup_armour(640 );
  set_damage_chance(7);
} /* setup() */
