// Autogenned file

inherit ARMOUR_OBJ;

void setup(){

  set_name("ring" );
  set_short("twined leaf silver wedding band" );
  add_alias("band" );
  add_adjective(({"twined", "leaf", "silver", "wedding" }) );
  set_main_plural("twined leaf silver wedding bands" );
  add_plural(({"bands", "rings"}) );
  set_long("A pattern of intertwining vine leaves is meticulously worked " "in sterling silver.  An heirloom pattern, this ring symbolizes the " "intertwining of souls: yours with your mate's.\n" );
  set_weight(1 );
  set_value(8000 );
  add_property("shop type", "jewellers" );
  add_property("no recycling", 1 );
  set_type("ring" );
  setup_armour(320 );
  set_damage_chance(10);
} /* setup() */
