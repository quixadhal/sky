// Autogenned file

inherit ARMOUR_OBJ;

void setup(){

  set_name("ring" );
  set_short("wedding ring" );
  add_adjective(({ "wedding" }) );
  add_alias(({ }) );
  add_plural(({  }) );
  set_long( "A beautiful gold band, it goes on and on forever "
            "symbolizing the eternal union of two people.   Engraved "
            "on the underside is a diagram of a fish and a rabbit in an "
            "eternal embrace.\n" );
  set_weight( 10 );
  set_value( 2000 );
  setup_armour( 15000 );
  add_immune_to(({  }) );
  set_type( "ring" );
  set_damage_chance( 2 );
  add_ac( "first", "blunt", 40 );
  add_ac( "second", "sharp", 30 );
  add_ac( "third", "pierce", 25);
} /* setup() */
