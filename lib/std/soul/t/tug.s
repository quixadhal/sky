(tug
  (pattern "<indirect:any-living>")
  (pattern "<string> <indirect:any-living>")
  (pattern "<indirect:any-living> <string>")
  (targeted
    (arguments
     (arguments sleeve,leg,arm,nose,ear,hair,naughty bits)
      (self "You tug on $mhcname$ $arg$ in a desperate attempt to get some attention.")
      (target "$mcname$ tugs on your $arg$ in a desperate attempt to get some attention.")
      (rest "$mcname$ tugs on $mhcname$ $arg$ in a desperate attempt to get some attention.")
    )
  )
)

/* Hobbes, 961030 */
