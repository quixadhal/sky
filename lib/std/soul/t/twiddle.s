(twiddle
  (pattern "<string>")
  (single
    (arguments
      (arguments thumbs,fingers,hair,nose)
      (self "You twiddle your $arg$.")
      (rest "$mcname$ twiddles $mposs$ $arg$.")
    )
  )
)

