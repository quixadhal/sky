// Autogenned file

inherit ARMOUR_OBJ;

void setup(){

  set_name("brooch" );
  set_short("occult bat brooch" );
  add_adjective(({ "occult", "bat" }) );
  add_plural("jewellery" );
  add_alias( "jewellery" );
  set_main_plural("occult bat brooches" );
  set_long( "This is a large brooch, cunningly crafted in the shape of a bat.  "
         "Eyes made of rubies stare into space, looking for wisdom.  Large "       
         "ears of spun gold collect knowledge from the cosmic rays sent "       
         "by the Elders, while wide-spread silver wings filter the earth-rays "       
         "and funnel their truth into a pyramid-shaped crystal set at the "       
         "heart of the bat's bronze body.\n" );
  set_weight( 5 );
  set_value( 10000 );
  setup_armour( 1000 );
  set_type( "badge" );
  set_damage_chance( 7 );
  add_property("shop type", "jewellers" );
  add_property("occult jewellery", 1);
} /* setup() */
